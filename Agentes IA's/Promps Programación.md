# PROMPTS PARA DESARROLLADORES DE SOFTWARE

## 🧠 Generación de Código

1. **Plantilla de código**: Genera un código de plantilla en `[lenguaje]` para una `[clase/módulo/componente]` llamada `[nombre]` con la siguiente funcionalidad: `[descripción de la funcionalidad]`.

2. **Función específica**: Crea una función en `[lenguaje]` para realizar `[operación]` en `[estructura de datos]` con los siguientes datos de entrada: `[variables de entrada]` y salida esperada: `[descripción de salida]`.

3. **Clase completa**: Genera una clase en `[lenguaje]` para una aplicación `[dominio]` que incluya métodos para `[lista de métodos]` y propiedades `[lista de propiedades]`.

4. **Patrón de diseño**: Basado en el `[patrón de diseño]`, crea un fragmento de código en `[lenguaje]` que demuestre su implementación para un `[caso de uso]`.

5. **Script con requisitos**: Escribe un script en `[lenguaje]` para realizar `[tarea]` usando `[biblioteca/framework]` con los siguientes requisitos: `[lista de requisitos]`.

## ✅ Completar Código

1. **Inicialización de estructuras**: En `[lenguaje]`, completa el siguiente fragmento de código que inicializa una `[estructura de datos]` con `[valores]`: `[fragmento de código]`.

2. **Función de cálculo**: Completa la función en `[lenguaje]` que calcula `[resultado deseado]` dados los siguientes parámetros de entrada: `[firma de la función]`.

3. **Llamada API**: Completa el código en `[lenguaje]` para realizar una llamada de API a `[punto de extremo de API]` con `[parámetros]` y procesar la respuesta: `[fragmento de código]`.

4. **Manejo de errores**: Completa el código en `[lenguaje]` para implementar el manejo de errores para la siguiente función: `[fragmento de código]`.

5. **Bucle completado**: Completa el siguiente bucle en `[lenguaje]` que itera sobre `[estructura de datos]` y realiza `[operación]`: `[fragmento de código]`.

## 🐞 Detección de Errores

1. **Identificación de errores**: Identifica posibles errores en el siguiente fragmento de código en `[lenguaje]`: `[fragmento de código]`.

2. **Prevención de errores**: Analiza el código en `[lenguaje]` dado y sugiere mejoras para prevenir `[tipo de error]`: `[fragmento de código]`.

3. **Fugas de memoria**: Encuentra posibles fugas de memoria en el siguiente código en `[lenguaje]` y sugiere correcciones: `[fragmento de código]`.

4. **Concurrencia**: Revisa el siguiente código en `[lenguaje]` en busca de condiciones de carrera o problemas de concurrencia: `[fragmento de código]`.

5. **Seguridad**: Revisa el siguiente código en `[lenguaje]` en busca de vulnerabilidades de seguridad: `[fragmento de código]`.

## 🔍 Revisión de Código

1. **Buenas prácticas**: Revisa el siguiente código en `[lenguaje]` en busca de buenas prácticas y sugiere mejoras: `[fragmento de código]`.

2. **Estilo de codificación**: Analiza el código en `[lenguaje]` dado para verificar el cumplimiento de las `[pautas de estilo de codificación]`: `[fragmento de código]`.

3. **Manejo de errores**: Verifica el siguiente código en `[lenguaje]` para el manejo adecuado de errores y sugiere mejoras: `[fragmento de código]`.

4. **Modularidad**: Evalúa la modularidad y mantenibilidad del siguiente código en `[lenguaje]`: `[fragmento de código]`.

5. **Rendimiento**: Evalúa el rendimiento del siguiente código en `[lenguaje]` y proporciona sugerencias de optimización: `[fragmento de código]`.

## 📝 Procesamiento de Lenguaje Natural

1. **Análisis de sentimientos**: Realiza un análisis de sentimientos en el siguiente texto: `[muestra de texto]`.

2. **Extracción de entidades**: Extrae entidades nombradas del siguiente texto: `[muestra de texto]`.

3. **Resumen automático**: Resume el siguiente artículo/documento: `[URL o muestra de texto]`.

4. **Identificación de temas**: Identifica el tema principal(es) del siguiente texto: `[muestra de texto]`.

5. **Palabras clave**: Realiza extracción de palabras clave en el siguiente texto: `[muestra de texto]`.

## 📚 Generación de Documentación de API

1. **Documentación API**: Genera la documentación de API para el siguiente código en `[lenguaje]`: `[fragmento de código]`.

2. **Referencia API**: Crea una referencia de API concisa para la clase en `[lenguaje]` dada: `[fragmento de código]`.

3. **Ejemplos de uso**: Genera ejemplos de uso para la siguiente API en `[lenguaje]`: `[fragmento de código]`.

4. **Entrada/Salida**: Documenta la entrada y salida esperadas para la función en `[lenguaje]` dada: `[fragmento de código]`.

5. **Guía rápida**: Produce una guía de inicio rápido para usar la siguiente biblioteca en `[lenguaje]`: `[fragmento de código]`.

## ⚡ Optimización de Consultas

1. **SQL Performance**: Optimiza la siguiente consulta SQL para mejorar el rendimiento: `[consulta SQL]`.

2. **Cuellos de botella**: Analiza la consulta SQL dada en busca de posibles cuellos de botella: `[consulta SQL]`.

3. **Indexación**: Sugiere estrategias de indexación para la siguiente consulta SQL: `[consulta SQL]`.

4. **JOIN vs Subconsultas**: Reescribe la siguiente consulta SQL para usar JOINs en lugar de subconsultas para mejorar el rendimiento: `[consulta SQL]`.

5. **NoSQL Optimization**: Optimiza la siguiente consulta NoSQL para un mejor rendimiento y uso de recursos: `[consulta NoSQL]`.

## 🤖 Chatbots y IA Conversacional

1. **Flujo conversacional**: Crea un flujo conversacional para un chatbot de soporte al cliente que maneje `[tipo de problema o consulta]`.

2. **Interacción personalizada**: Diseña una interacción de chatbot que ayude a los usuarios a encontrar `[producto o servicio]` según sus preferencias y requisitos.

3. **Guión conversacional**: Desarrolla un guion conversacional para un chatbot que guíe a los usuarios a través del `[proceso de incorporación o configuración de funciones]`.

4. **FAQ Bot**: Implementa un chatbot que pueda responder preguntas frecuentes sobre `[tema o dominio]`.

5. **Interfaz natural**: Crea una interfaz de lenguaje natural para un chatbot que permita a los usuarios realizar `[tarea o operación específica]` mediante comandos de voz o entrada de texto.

## 🎨 Diseño de Interfaz de Usuario

1. **Boceto UI**: Genera un boceto de interfaz de usuario para una aplicación `[web/móvil]` que se centre en `[objetivo o tarea del usuario]`.

2. **Mejoras UI**: Sugiere mejoras para la interfaz de usuario existente de `[aplicación o sitio web]` para mejorar `[usabilidad, accesibilidad o estética]`.

3. **UI Responsiva**: Diseña una interfaz de usuario receptiva para una aplicación `[web/móvil]` que se adapte a diferentes tamaños y orientaciones de pantalla.

4. **Wireframe**: Crea un wireframe para una aplicación `[web/móvil]` que optimice los flujos de trabajo del usuario para `[caso de uso específico]`.

5. **Librería de componentes**: Diseña una biblioteca de componentes de interfaz de usuario para una aplicación `[web/móvil]` que cumpla con `[sistema de diseño o guía de estilo]`.

## 🧪 Pruebas Automatizadas

1. **Casos de prueba**: Genera casos de prueba para la siguiente función en `[lenguaje]` basándote en los parámetros de entrada y la salida esperada: `[firma de la función]`.

2. **Script de prueba**: Crea un script de prueba para el código en `[lenguaje]` dado que cubra las pruebas `[unitarias/integración/sistema]`: `[fragmento de código]`.

3. **Datos de prueba**: Genera datos de prueba para la siguiente función en `[lenguaje]` que pruebe varios casos límite: `[firma de la función]`.

4. **Estrategia de prueba**: Diseña una estrategia de prueba para una aplicación `[web/móvil]` que incluya pruebas `[unitarias, integración, sistema y/o rendimiento]`.

5. **Pruebas API**: Escribe un conjunto de pruebas para una API en `[lenguaje]` que verifique su funcionalidad y rendimiento bajo diferentes condiciones.

## 🔧 Refactorización de Código

1. **Mejoras de legibilidad**: Sugiere mejoras de refactorización para el siguiente código en `[lenguaje]` para mejorar la legibilidad y mantenibilidad: `[fragmento de código]`.

2. **Patrones de diseño**: Identifica oportunidades para aplicar `[patrón de diseño]` en el código en `[lenguaje]` dado: `[fragmento de código]`.

3. **Optimización de performance**: Optimiza el siguiente código en `[lenguaje]` para mejorar su rendimiento: `[fragmento de código]`.

4. **Modularidad**: Refactoriza el código en `[lenguaje]` dado para mejorar su modularidad y reutilización: `[fragmento de código]`.

5. **Cumplimiento de estándares**: Propone cambios para el código en `[lenguaje]` dado para cumplir con `[estilo de codificación o mejores prácticas]`: `[fragmento de código]`.

## 🧮 Desarrollo de Algoritmos

1. **Algoritmo óptimo**: Sugiere un algoritmo óptimo para resolver el siguiente problema: `[descripción del problema]`.

2. **Eficiencia mejorada**: Mejora la eficiencia del algoritmo dado para `[caso de uso específico]`: `[algoritmo o pseudocódigo]`.

3. **Big Data**: Diseña un algoritmo que pueda manejar `[datos a gran escala o alta demanda]` para `[tarea u operación específica]`.

4. **Paralelización**: Propone una versión paralela o distribuida del siguiente algoritmo para mejorar el rendimiento: `[algoritmo o pseudocódigo]`.

5. **Complejidad**: Evalúa la complejidad temporal y espacial del algoritmo dado y sugiere optimizaciones: `[algoritmo o pseudocódigo]`.

## 🌐 Traducción de Código

1. **Traducción básica**: Traduce el siguiente código en `[lenguaje de origen]` a `[lenguaje de destino]`: `[fragmento de código]`.

2. **Conversión de clases**: Convierte la clase o módulo en `[lenguaje de origen]` dado a `[lenguaje de destino]` manteniendo su funcionalidad y estructura: `[fragmento de código]`.

3. **Migración de frameworks**: Migra el siguiente código en `[lenguaje de origen]` que utiliza `[biblioteca o framework]` a `[lenguaje de destino]` con una biblioteca o framework similar: `[fragmento de código]`.

4. **Equivalencia de rendimiento**: Reescribe el siguiente algoritmo en `[lenguaje de origen]` en `[lenguaje de destino]` con características de rendimiento equivalentes: `[algoritmo o pseudocódigo]`.

5. **Adaptación a mejores prácticas**: Adapta el siguiente fragmento de código en `[lenguaje de origen]` a `[lenguaje de destino]` cumpliendo con las mejores prácticas del `[lenguaje de destino]`: `[fragmento de código]`.

## 📋 Análisis de Requisitos

1. **Arquitectura de alto nivel**: Interpreta los siguientes requisitos del proyecto y sugiere una arquitectura o diseño de alto nivel: `[descripción de los requisitos]`.

2. **Identificación de riesgos**: Identifica riesgos o desafíos potenciales en la implementación de los requisitos del proyecto dados: `[descripción de los requisitos]`.

3. **Priorización**: Sugiere una estrategia de priorización para la siguiente lista de requisitos del proyecto: `[lista de requisitos]`.

4. **Tecnología recomendada**: Basado en los requisitos del proyecto dados, recomienda un lenguaje de programación, framework o tecnología adecuada: `[descripción de los requisitos]`.

5. **Estimación de esfuerzo**: Estima el esfuerzo de desarrollo y los recursos necesarios para implementar los siguientes requisitos del proyecto: `[descripción de los requisitos]`.

## 📅 Planificación del Proyecto

1. **Cronograma**: Estima el cronograma y los hitos para un proyecto con los siguientes requisitos: `[descripción de los requisitos]`.

2. **Metodología**: Propone una metodología de desarrollo (por ejemplo, Ágil, Scrum, Cascada) para un proyecto con las siguientes características: `[descripción del proyecto]`.

3. **Estructura de equipo**: Sugiere una estructura de equipo y roles para un proyecto con el siguiente alcance y requisitos: `[descripción del proyecto]`.

4. **Dependencias**: Identifica dependencias y posibles cuellos de botella en un proyecto con los siguientes requisitos y restricciones: `[descripción de los requisitos]`.

5. **Plan de alto nivel**: Desarrolla un plan de proyecto de alto nivel que incluya tareas, recursos y plazos para un proyecto con los siguientes objetivos: `[objetivos del proyecto]`.

## 🔧 Seguimiento y Resolución de Problemas

1. **Categorización automática**: Categoriza y prioriza automáticamente la siguiente lista de problemas reportados: `[lista de problemas]`.

2. **Soluciones potenciales**: Sugiere soluciones potenciales para el siguiente problema reportado: `[descripción del problema]`.

3. **Causa raíz**: Identifica la causa raíz del problema dado y propone pasos para prevenir su recurrencia: `[descripción del problema]`.

4. **Estimación de esfuerzo**: Estima el esfuerzo requerido para resolver el siguiente problema y su impacto en el cronograma del proyecto: `[descripción del problema]`.

5. **Solución temporal**: Propone una solución alternativa o solución temporal para el siguiente problema crítico mientras se desarrolla una solución permanente: `[descripción del problema]`.

## 📊 Visualización de Código

1. **Diagrama UML**: Genera un diagrama UML para el siguiente código en `[lenguaje]`: `[fragmento de código]`.

2. **Diagrama de flujo**: Crea un diagrama de flujo o representación visual del siguiente algoritmo en `[lenguaje]`: `[algoritmo o pseudocódigo]`.

3. **Grafo de llamadas**: Visualiza el grafo de llamadas o dependencias del siguiente código en `[lenguaje]`: `[fragmento de código]`.

4. **Flujo de datos**: Genera un diagrama de flujo de datos para el código en `[lenguaje]` dado que demuestre cómo se procesan los datos: `[fragmento de código]`.

5. **Visualización interactiva**: Crea una visualización interactiva del comportamiento en tiempo de ejecución o rendimiento del siguiente código en `[lenguaje]`: `[fragmento de código]`.

## 📈 Visualización de Datos

1. **Gráfico de barras**: Genera un gráfico de barras que represente los siguientes datos: `[descripción de los datos o conjunto de datos]`.

2. **Gráfico de líneas**: Crea un gráfico de líneas que visualice la tendencia en los siguientes datos de series temporales: `[descripción de los datos o conjunto de datos]`.

3. **Mapa de calor**: Diseña un mapa de calor que represente la correlación entre las siguientes variables: `[lista de variables]`.

4. **Histograma**: Visualiza la distribución del siguiente conjunto de datos usando un histograma o un diagrama de caja: `[descripción de los datos o conjunto de datos]`.

5. **Diagrama de dispersión**: Genera un diagrama de dispersión que demuestre la relación entre las siguientes dos variables: `[variable 1]` y `[variable 2]`.

## 🛠️ Prototipado

1. **Prueba de concepto**: Genera un código de prueba de concepto en `[lenguaje]` para una `[idea de proyecto o funcionalidad]` basado en los siguientes requisitos: `[descripción de los requisitos]`.

2. **Prototipo funcional**: Crea un prototipo funcional de una aplicación `[web/móvil]` que demuestre `[funcionalidad específica o flujo de usuario]`.

3. **Producto mínimo viable**: Desarrolla un producto mínimo viable (MVP) para un `[producto o servicio]` basado en las siguientes especificaciones: `[descripción de las especificaciones]`.

4. **Simulación**: Implementa una simulación o modelo simple de un `[sistema o proceso]` usando `[lenguaje o tecnología]`.

5. **Demostración funcional**: Crea una demostración funcional de una `[herramienta o función]` que muestre sus beneficios potenciales y casos de uso.

## 👥 Codificación Colaborativa

1. **Revisión de código**: Facilita una sesión de revisión de código entre `[miembro del equipo 1]` y `[miembro del equipo 2]` para el siguiente código en `[lenguaje]`: `[fragmento de código]`.

2. **Programación en pareja**: Establece una sesión de programación en pareja entre `[miembro del equipo 1]` y `[miembro del equipo 2]` para implementar `[funcionalidad o característica específica]`.

3. **Lluvia de ideas**: Organiza una sesión de lluvia de ideas para generar ideas y soluciones para `[problema o desafío]` enfrentado por el equipo de desarrollo.

4. **Comunicación técnica**: Ayuda a establecer un canal de comunicación entre `[miembro del equipo 1]` y `[miembro del equipo 2]` para discutir y resolver `[problema técnico o pregunta]`.

5. **Integración de código**: Asiste en coordinar una fusión o integración de código entre el trabajo de `[miembro del equipo 1]` y el trabajo de `[miembro del equipo 2]`.

## 🔍 Análisis de Código

1. **Dependencias frecuentes**: Analiza la base de código dada para identificar bibliotecas o dependencias utilizadas con frecuencia: `[URL del repositorio o descripción de la base de código]`.

2. **Reporte de calidad**: Genera un informe sobre la complejidad y mantenibilidad de la siguiente base de código: `[URL del repositorio o descripción de la base de código]`.

3. **Tendencias de desarrollo**: Identifica tendencias o patrones en el historial de desarrollo de la base de código dada: `[URL del repositorio o descripción de la base de código]`.

4. **Áreas de mejora**: Analiza la base de código para identificar áreas potenciales de mejora o refactorización: `[URL del repositorio o descripción de la base de código]`.

5. **Estilos de codificación**: Genera un resumen de los estilos y convenciones de codificación utilizados en la siguiente base de código: `[URL del repositorio o descripción de la base de código]`.

## 🎯 Sugerencias de Patrones de Diseño

1. **Patrón adecuado**: Basado en el código en `[lenguaje]` dado, recomienda un patrón de diseño adecuado para mejorar su estructura: `[fragmento de código]`.

2. **Oportunidades de aplicación**: Identifica oportunidades para aplicar el `[patrón de diseño]` en la siguiente base de código en `[lenguaje]`: `[URL del repositorio o descripción de la base de código]`.

3. **Alternativas**: Sugiere un patrón de diseño alternativo para el código en `[lenguaje]` dado que pueda proporcionar beneficios adicionales: `[fragmento de código]`.

4. **Solución específica**: Explica cómo se puede aplicar el `[patrón de diseño]` en el código en `[lenguaje]` dado para abordar `[problema o desafío específico]`: `[fragmento de código]`.

5. **Comparación**: Compara los pros y los contras de usar `[patrón de diseño 1]` vs `[patrón de diseño 2]` en el contexto del código en `[lenguaje]` dado: `[fragmento de código]`.

## ⚡ Optimización del Rendimiento

1. **Cuellos de botella**: Identifica cuellos de botella de rendimiento en el código en `[lenguaje]` dado y sugiere optimizaciones: `[fragmento de código]`.

2. **Uso de memoria**: Propone cambios al código en `[lenguaje]` dado para mejorar su uso de memoria: `[fragmento de código]`.

3. **Paralelización**: Sugiere formas de paralelizar o distribuir el siguiente código en `[lenguaje]` para mejorar su rendimiento: `[fragmento de código]`.

4. **Comparación de técnicas**: Compara el rendimiento del código en `[lenguaje]` dado usando diferentes técnicas de optimización o bibliotecas: `[fragmento de código]`.

5. **Análisis multiplataforma**: Analiza el rendimiento del siguiente código en `[lenguaje]` en diferentes entornos o configuraciones de hardware: `[fragmento de código]`.

## 🔐 Seguridad y Privacidad

1. **Evaluación de seguridad**: Evalúa la seguridad del código en `[lenguaje]` dado y sugiere mejoras: `[fragmento de código]`.

2. **Riesgos de privacidad**: Identifica posibles riesgos de privacidad en el siguiente código en `[lenguaje]` y recomienda estrategias de mitigación: `[fragmento de código]`.

3. **Protección contra amenazas**: Propone cambios al código en `[lenguaje]` dado para mejorar su resistencia a amenazas de seguridad comunes (por ejemplo, inyección de SQL, XSS, CSRF): `[fragmento de código]`.

4. **Cumplimiento normativo**: Analiza la seguridad del código en `[lenguaje]` dado en el contexto de `[normas o regulaciones específicas de la industria]`: `[fragmento de código]`.

5. **Cifrado**: Sugiere algoritmos de cifrado o hashing para asegurar datos sensibles en el código en `[lenguaje]` dado: `[fragmento de código]`.

## ♿ Accesibilidad e Inclusión

1. **Evaluación WCAG**: Evalúa la accesibilidad de la aplicación `[web/móvil]` dada y sugiere mejoras para cumplir con las pautas WCAG: `[URL de la aplicación o descripción]`.

2. **Usabilidad inclusiva**: Propone cambios a la aplicación `[web/móvil]` dada para mejorar su usabilidad para usuarios con `[discapacidad o impedimento específico]`: `[URL de la aplicación o descripción]`.

3. **Contenido inclusivo**: Sugiere formas de hacer que la aplicación `[web/móvil]` dada sea más inclusiva y diversa en términos de contenido, imágenes e idioma: `[URL de la aplicación o descripción]`.

4. **Accesibilidad multi-dispositivo**: Analiza la accesibilidad de la aplicación `[web/móvil]` dada en varios dispositivos y tamaños de pantalla: `[URL de la aplicación o descripción]`.

5. **Herramientas de accesibilidad**: Recomienda herramientas o bibliotecas para ayudar a mejorar la accesibilidad e inclusión de la aplicación `[web/móvil]` dada: `[URL de la aplicación o descripción]`.

## 🚀 DevOps y CI/CD

1. **Pipeline CI/CD**: Diseña un pipeline CI/CD para el proyecto en `[lenguaje]` basado en sus requisitos y restricciones: `[descripción del proyecto]`.

2. **Automatización de despliegue**: Propone una estrategia para automatizar el despliegue de la aplicación en `[lenguaje]` en `[proveedor de nube o entorno]`: `[descripción de la aplicación]`.

3. **Optimización de builds**: Sugiere mejoras para el proceso de compilación y despliegue del proyecto en `[lenguaje]` para aumentar la eficiencia: `[descripción del proyecto]`.

4. **Contenerización**: Compara las ventajas y desventajas de diferentes tecnologías de contenerización (por ejemplo, Docker, Kubernetes, Podman) para el proyecto en `[lenguaje]`: `[descripción del proyecto]`.

5. **Cloud native**: Identifica oportunidades para optimizar la infraestructura y el uso de recursos del proyecto en `[lenguaje]` utilizando tecnologías nativas de la nube: `[descripción del proyecto]`.

## 🌐 Trabajo Remoto y Colaboración

1. **Herramientas colaborativas**: Sugiere herramientas y mejores prácticas para la colaboración remota entre los miembros de un equipo de desarrollo en `[lenguaje]`.

2. **Comunicación distribuida**: Propone estrategias para mejorar la comunicación y coordinación entre los miembros del equipo distribuido que trabajan en un proyecto en `[lenguaje]`.

3. **Gestión de tareas**: Recomienda un flujo de trabajo para gestionar y priorizar tareas para un equipo de desarrollo remoto en `[lenguaje]`.

4. **Motivación del equipo**: Sugiere formas de mantener el ánimo y la motivación del equipo de desarrollo remoto en `[lenguaje]` durante un proyecto a largo plazo.

5. **Reuniones efectivas**: Comparte consejos para organizar y facilitar reuniones remotas efectivas para un equipo de desarrollo en `[lenguaje]`.

## 🌟 Contribución a Código Abierto

1. **Proyectos adecuados**: Identifica proyectos de código abierto en `[lenguaje]` adecuados para un desarrollador con `[habilidades o intereses específicos]`.

2. **Issues y features**: Sugiere problemas abiertos o solicitudes de funciones en el siguiente proyecto de código abierto en `[lenguaje]` que coincidan con mi conjunto de habilidades: `[URL del repositorio o descripción del proyecto]`.

3. **Mejores prácticas**: Recomienda mejores prácticas para contribuir a proyectos de código abierto en `[lenguaje]` como un contribuyente nuevo o inexperto.

4. **Navegación en proyectos**: Proporciona orientación sobre cómo navegar por el código y el proceso de desarrollo del proyecto de código abierto en `[lenguaje]` dado: `[URL del repositorio o descripción del proyecto]`.

5. **Pull requests**: Explica cómo preparar y enviar una solicitud de extracción para el proyecto de código abierto en `[lenguaje]` dado: `[URL del repositorio o descripción del proyecto]`.

## 📖 Documentación Técnica

1. **Referencia API**: Escribe una referencia de API para el siguiente código en `[lenguaje]`: `[fragmento de código]`.

2. **Guía de usuario**: Crea una guía de usuario para el `[software o herramienta]` dada que cubra la instalación, configuración y uso básico.

3. **Plan de pruebas**: Escribe un plan de pruebas completo para el siguiente código en `[lenguaje]`, incluyendo casos de prueba y escenarios: `[fragmento de código]`.

4. **FAQ**: Desarrolla una sección de preguntas frecuentes que aborde preguntas comunes y problemas relacionados con el proyecto o herramienta en `[lenguaje]`.

5. **Arquitectura del sistema**: Produce un resumen claro y conciso de la arquitectura y diseño del proyecto o sistema en `[lenguaje]`: `[descripción del proyecto]`.

## 🏗️ Diseño y Desarrollo de API

1. **Diseño API**: Diseña una API para una `[tipo de aplicación o servicio]` que admita las siguientes operaciones: `[lista de operaciones]`.

2. **API RESTful**: Propone una estructura de API RESTful para el siguiente código en `[lenguaje]` que siga las mejores prácticas: `[fragmento de código]`.

3. **Mejoras de diseño**: Sugiere mejoras al diseño de API siguiente para mejorar su usabilidad, rendimiento o seguridad: `[descripción de la API]`.

4. **Cliente API**: Escribe un código en `[lenguaje]` para interactuar con la siguiente API: `[documentación o referencia de la API]`.

5. **Autenticación**: Compara diferentes mecanismos de autenticación y autorización de API (por ejemplo, OAuth, JWT, claves de API) para el proyecto en `[lenguaje]`: `[descripción del proyecto]`.